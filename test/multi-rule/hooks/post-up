#!/usr/bin/env bash
set -euo pipefail

# Detect default router for the lan-ip check
router=$(ip route show default | awk '{print $3; exit}')

# Run the check from inside the VM with both flags
migrant ssh -- "uv run ~/netcheck.py --no-interactive --host-port tcp/9999 --lan-ip $router"
